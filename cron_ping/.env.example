# Copy to .env for local testing only. Never commit a real .env file.
# In GitHub Actions these come from repository Secrets (see the
# cron_ping.yml workflow) instead.

API_URL=https://your-site.example.com/api/your-endpoint
API_KEY=your_api_key_here

# Optional: GET (default) or POST
API_METHOD=GET
